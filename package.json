{
  "name": "vivy-payment-server",
  "version": "1.0.0",
  "description": "Vivy 💜 payment backend — Express port of the original Firebase Cloud Functions (Flutterwave verification, webhook, Coin crediting). Deployed on Render; frontend stays on GitHub Pages, auth/data stay on Firebase.",
  "main": "server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "firebase-admin": "^12.6.0"
  },
  "private": true
}
